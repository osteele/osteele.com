Publish:
- backspace: only collect most recent dead
- add to wikipedia?

Polish:
- scroll bar
- simplify a+
- roundrect scrim

Bugs:
- "a|" doesn't match empty string
- [ab]d|[bc]e|ab and input a, the final b in the pattern isn't underlined

Codeliness:
- x, y -> pos


Bezier:
- try the midpoint thingie:
if the midpoint of the subdivision is close to the midpoint of the bezier, draw the quadratic


Graphviz:
- try the other dot format
- look at nested graphs
- add a ruby parser
- create an example
- parameterize the bin location


Rework:
- dotall
- java?

Dotall:
- ruby: dotall is /m
- add dotall; js implementation uses \u1234

Polish:
- scrim while loading graph
- bigger, smaller button for text area

Corners:
- server error

Literacy:
- js error in prototype on startup
  
Debugging:
  document={location: 'http://osteele.com//'};
  function $(){}
  load('rework.js');

     <input name="complement" onclick="new Ajax.Request('server.py', {asynchronous:true, onComplete:handle, parameters:Form.serialize(this.form)+'&amp;operation=complement'}); return false;" type="button" value="Complement" />
     <span id="rr"></span>

Java:
Java myString.matches("regex") entire string
myString.replaceAll("regex", "replacement") 
myString.split("regex")
Pattern.compile("regex", Pattern.CASE_INSENSITIVE | Pattern.DOTALL | Pattern.MULTILINE)
myPattern.split("subject")
more: http://www.regular-expressions.info/java.html

PHP:
- ereg http://www.regular-expressions.info/php.html

More:
- \A, \Z

- test match, split, scan on empty in various languages
- test: $1
- disable update button on invalid pattern